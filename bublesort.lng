num i, j, n, swap;
arr a;
{
    in n;
    mem1 a n;
    i := 0;
    while i < n {
        in a[i];
        i := i + 1
    };
    i := 0;
    while i < n {
        j:=0;
        while j < n - 1{
            if a[j] > a[(j + 1)]{
                swap := a[(j+1)];
                a[(j+1)] := a[j];
                a[j] := swap
            };
            j := j + 1
        };
        i := i + 1
    };
    out 3212213124;
    i := 0;
    while i < n {
        out a[i];
        i := i + 1
    }
}
